<div class="container">
  <%= link_to svg_tag('left-arrow.svg', class: 'icon left-arrow'), :back %>
  <h4> <%= @post.title %> </h4>
  <h6> <%= format_date @post.created_at %> </h6>
  <p> <%= markdown @post.body %> </p>
</div>
